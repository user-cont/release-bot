#!/bin/sh

cat <<EOF
This is a S2I release-bot image:
To use it, install S2I: https://github.com/openshift/source-to-image
Sample invocation:
s2i build <configuration_repository> usercont/release-bot:dev release-bot-app:dev
You can then run the resulting image via:
docker run release-bot-app:dev
EOF
